# ローカル画像認識版 (2026-09-16)

- 23/34をQwen2.5-VL/Ollamaによる実画像入力・構造化出力へ変更。
- bbox・根拠・信頼度・推定寸法・画像SHA256を保存。モデル出力の検証と有限修復を追加。
- Mesh/Gaussianとアセットプロンプトをモデル判断から引き継ぐ。新形式の明示unity_typeを尊重。
- prepareで家具CSVと背景プロンプトを直接出力。画像由来情報を共通sceneへ保持。
- 旧00/01/02/17/18/19/20/21/24/25/26/27/35を削除し、必要な実行経路へ絞った。
- 重複requirementsをruntimeとlockに統一。ローカルモデル導入・起動手順を追加。
- 手順書と動作原理解説を全面更新。元のWSL/Unityプロジェクトは変更しない。

# 旧V2 (2026-09-16、以下は過去の変更履歴)

## 新規

- 共通schema 2.0、入力検証、Unity/ジョブへの変換、アセット存在検査。
- Gaussianの引数配列による実行計画と失敗時停止。
- Unityのroom-local家具ソルバー、支持グループ、最終監査と評価カバー率。
- RendererのないGaussian用Boundsアダプタと明示窓参照。
- 窓内/全面被覆の式を分離した背景配置、カメラ検証待ち状態。
- 外部TRELLIS実行器のアダプタ、背景画像生成、任意の部屋生成。

## 既存コードから変更

- 07_5：seed、checkpointのrender_settings、alpha/projection損失。
- 08：checkpoint設定の継承、可視性なしの検証。
- 09：有効スケールの出力とメタデータ。
- 12：Bounds取得前の依存グラフ更新、複数ルートをまとめてyaw回転。
- 17：V2入力の検証分岐。
- 20：共通runnerへの入口に置き換え。
- 23：room.sizeに合わせた床・天井・壁の寸法/位置。
- 35：V2を固定座標の旧ソルバーへ渡す誤用を拒否。

## 引き継ぎ

既存24本のコピーを基にし、元プロジェクトで確認した05（多視点描画）・12（正規化）も同梱しました。元プロジェクト内のファイルは変更していません。依存モデル・GPU互換性・実際のGaussian表示は各環境での確認が必要です。

## 2026-09-16 物体別レビュー
23を短い検出＋切り抜き付き個別確認に変更。寸法名・支持先・固定アンカー・人の訂正を保存。34をsource別生成と画像レビュー、限定修復、成功キャッシュへ変更。不完全出力と古いシーンの制約をprepareで拒否。OBJECT_REVIEW.mdに手順を追加。

## 2026-09-16 支持先と固定窓の修復
支持面を独立検証し、古いscene_graphをバックアップして修復。窓枠を可動家具として認めない。--estimate-fixedで窓の推定配置案を生成。制約最大4件、左右両壁への同時接触禁止、古い不正キャッシュ再検証、unknown回答は再試行。
